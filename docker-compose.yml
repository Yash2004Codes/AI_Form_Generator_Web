version: "3.8"
services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.docker   # create this (see below)
    depends_on:
      - mongo
    restart: unless-stopped

  
volumes:
  mongo_data:
